# Database Configuration (Supabase)
DATABASE_URL=your_supabase_connection_string_here

# JWT Secret (generated)
JWT_SECRET=fc064c38b362c865b829e7f1f31cb84426b6b645bf2d69af6279dbb6c54e5b5b

# Server Configuration
PORT=3000

# Public web menu base URL (no trailing slash) — used to build the QR target:
# <MENU_BASE_URL>/menu/<shopId>.
#   Local web dev server:   http://localhost:5173
#   ngrok tunnel (example): https://your-subdomain.ngrok-free.app
# ngrok URLs change on each restart — update MENU_BASE_URL and regenerate QR.
MENU_BASE_URL=http://localhost:5173
