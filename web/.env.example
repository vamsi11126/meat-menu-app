# Base URL of the backend API. Defaults to http://localhost:5000 when unset.
# Set this to the Railway backend URL after deploying the backend in Phase 4.
# VITE_API_URL=https://your-railway-backend-url.railway.app
